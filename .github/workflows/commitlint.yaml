name: Commit Lint

on:
  pull_request:
    types: [opened, edited, synchronize, reopened]
  merge_group: # Required for merge queue to work

jobs:
  commitlint:
    uses: KeepTruckin/github-workflows-catalog/.github/workflows/commitlint.yaml@master
    with:
      node_version: "20"
